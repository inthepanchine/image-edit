// Interfaces of `CanvasShape` class.

/** Representation of a shape style. */
export interface ShapeStyle {
	/** Stroke color. */
	stroke: string;
	/** Stroke width. */
	strokeWidth: number;
	/** Fill color. */
	fill: string;
}
